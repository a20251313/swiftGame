// Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"
let triple:Int->Int = {
    (number:Int) in
    let result = 3 * number
    number;
    return number;
}
triple(3)
let listOfnumbers = 1...5
var sum = 0
for atNum in listOfnumbers
{
    sum += atNum;
}
sum

var j = 2;
for var i = 0;i < 5;++i{
    j += j*i
}
j

let frame = CGRect(x: 0, y: 0, width: 150, height: 150);
//let customView = CheckBox(frame:frame);

